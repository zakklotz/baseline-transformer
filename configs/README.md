# Configs

## Parity configs
`configs/parity/wt103_512d.yaml` sets canonical transformer dimensions for fair comparisons:
- vocab_size=50257
- max_seq_len=512
- d_model=512, n_heads=8, d_ff=2048, n_layers=6

These are useful as a “known baseline shape” for other model projects.
