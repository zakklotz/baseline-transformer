# baseline-transformer

A reusable transformer baseline project built on `nn-core`.

## Goals
- Provide a clean transformer baseline harness (configs, data, train/eval)
- Keep interfaces stable so future models can be compared fairly

## Quickstart
(Coming soon)
